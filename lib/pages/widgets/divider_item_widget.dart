import 'package:flutter/material.dart';

class DividerItemWidget extends StatelessWidget {
  const DividerItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 1,color: Colors.grey,);
  }
}